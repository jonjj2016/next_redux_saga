import { types } from '../types';

const initState = {
    posts : [],
    post : {},
    loading: false,
    error: null
}

export default (state= initState, action) => {
  switch(action.types){
      default :
      return state
  }
};
